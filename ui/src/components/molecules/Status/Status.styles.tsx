import styled from 'styled-components';

const StyledStatusWrapper = styled.div`
  display: flex;
  align-items: center;
  width: 60px;
  justify-content: flex-start;
`;

export default StyledStatusWrapper;
